![Work Report](../../image/baner/Untitled-1.jpg)

---
<h3 align="right"><b>فصل دوم 🔹</b></h3>
<h3 align="right"><b>قسمت:هفتم 🎬</b></h3>
<h3 align="right"><b>۱۴۰۴/۰۹/۲۷ 📆</b></h3>
<h3 align="right"><b>"Class and Style"</b></h3>

---
###### Home
> [توضیحات](#1) - [چیزهایی که یاد گرفتم](#2) - [سوالات و مشکلات](#3) - [کد ها و خروجی ها](#4)

---
###### 1
### 🔍 توضیحات
- در این تمرین با مدیریت کلاس‌ها و استایل عناصر در DOM آشنا شدم. با استفاده از classList می‌توان کلاس‌های یک عنصر را به‌صورت داینامیک کنترل کرد؛ متد add() برای اضافه کردن کلاس جدید، remove() برای حذف کلاس، toggle() برای روشن/خاموش کردن یک کلاس (مثل مخفی و نمایش دادن عنصر) و contains() برای بررسی وجود یک کلاس خاص استفاده می‌شود. همچنین برای تغییر استایل مستقیم عناصر از elem.style استفاده کردم که امکان تغییر ویژگی‌هایی مانند backgroundColor را به‌صورت inline فراهم می‌کند. علاوه بر آن با مفهوم getComputedStyle() آشنا شدم که برای دریافت استایل نهایی یک عنصر (چه از CSS و چه inline) کاربرد دارد. در مثال عملی، با کلیک روی دکمه‌ها، یک div با استفاده از کلاس hidden مخفی و نمایش داده شد و رنگ پس‌زمینه یک عنصر دیگر نیز به‌صورت داینامیک تغییر کرد.
> [Home](#home)
---
###### 2
## 🧠 چیزهایی که یاد گرفتم
- استفاده از `classList` برای مدیریت کلاس‌های عناصر
- اضافه کردن کلاس با `elem.classList.add()`
- حذف کلاس با `elem.classList.remove()`
- تغییر وضعیت کلاس با `elem.classList.toggle()`
- بررسی وجود کلاس با `elem.classList.contains()`
- تغییر استایل inline با `elem.style`
- تفاوت استایل inline و استایل CSS
- دریافت استایل نهایی عنصر با `getComputedStyle()`
- پیاده‌سازی تعامل کاربر با دکمه و JavaScript

> [Home](#home)

###### 3
## سوالات و مشکلات⁉️
> [Home](#home)
---
###### 4
## 🧪 تست‌ها و خروجی‌ها
[Code File](main.html)

> [Home](#home)